# OpenClaw Agent Loop

The runnable demo lives in the archive (self-contained):

```bash
cd archive/harbor-builder-workspace
HARBOR_DEMO=1 python examples/openclaw_agent_loop/run.py
```

After you move `archive/harbor-builder-workspace/` out, run it from that folder.

Fresh-project copy will land here.
